@model IEnumerable<HealthyRecipes.Models.Ingredient>

@{
    ViewData["Title"] = "Index";
}

<h2>Ingredients</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaloriesPerItem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProteinsPerItem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SugarPerItem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IngredientFact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CaloriesPerItem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProteinsPerItem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SugarPerItem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IngredientFact)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@
<div class="center-div">
    <div class="card-columns">
        @foreach (var item in Model)
        {
            <div class="card">

                <div class="card-body">
                    <h5 class="card-title">@Html.DisplayFor(modelItem => item.Name)</h5>
                </div>
                <div class="card-img"><img src="@Html.DisplayFor(modelItem => item.ImageUrl)" width="355" height="200" /></div>
                <div class="card-footer">
                    Proteins per item:
                    @Html.DisplayFor(modelItem => item.ProteinsPerItem) grams<br />
                    Calories per item:
                    @Html.DisplayFor(modelItem => item.CaloriesPerItem) <br />
                    Sugar per item:
                    @Html.DisplayFor(modelItem => item.SugarPerItem) grams
                    <br /><br />
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>


                </div>
            </div>


        }
    </div>
</div>
